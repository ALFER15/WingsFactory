<section class="hero-three">
  <div class="left">
    <p class="badge">Split Narrative</p>
    <h1>Una mitad vende, la otra demuestra</h1>
    <p>
      Diseño con línea divisoria central: copy contundente del lado izquierdo y carrusel visual del lado derecho.
      Ideal para explicar propuesta + mostrar estilo de marca.
    </p>
    <a href="#">Probar estructura</a>
  </div>

  <div class="divider" aria-hidden="true"></div>

  <div class="right">
    <div class="slides" aria-hidden="true">
      <article>Panel de analítica en vivo</article>
      <article>Vista de componentes reutilizables</article>
      <article>Sistema de secciones escalable</article>
    </div>
  </div>
</section>

<style>
  .hero-three {
    display: grid;
    grid-template-columns: 1fr auto 1fr;
    gap: 1.2rem;
    align-items: stretch;
    padding: clamp(2rem, 5vw, 4rem);
    border-radius: 26px;
    background: linear-gradient(135deg, #fff7ed, #f8fafc);
    border: 1px solid #fed7aa;
  }

  .left {
    display: grid;
    align-content: center;
    gap: 1rem;
  }

  .badge {
    margin: 0;
    color: #9a3412;
    font-weight: 700;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    font-size: 0.78rem;
  }

  h1 {
    margin: 0;
    font-size: clamp(1.8rem, 4vw, 3rem);
    line-height: 1.1;
    color: #7c2d12;
  }

  p {
    margin: 0;
    color: #7c2d12;
    max-width: 44ch;
  }

  a {
    justify-self: start;
    text-decoration: none;
    color: #fff;
    background: #ea580c;
    padding: 0.72rem 1.1rem;
    border-radius: 999px;
    font-weight: 600;
  }

  .divider {
    width: 2px;
    background: linear-gradient(#fdba74, #f97316, #fdba74);
    border-radius: 999px;
  }

  .right {
    min-height: 270px;
    overflow: hidden;
    border-radius: 18px;
    border: 1px solid #fed7aa;
    background: #fff;
  }

  .slides {
    display: grid;
    height: 300%;
    animation: carouselY 11s infinite;
  }

  .slides article {
    display: grid;
    place-content: center;
    font-weight: 700;
    color: #9a3412;
    background: linear-gradient(135deg, #fff7ed, #ffe8cc);
    padding: 1.4rem;
    text-align: center;
  }

  @keyframes carouselY {
    0%, 20% { transform: translateY(0); }
    33%, 53% { transform: translateY(-33.333%); }
    66%, 86% { transform: translateY(-66.666%); }
    100% { transform: translateY(0); }
  }

  @media (max-width: 860px) {
    .hero-three {
      grid-template-columns: 1fr;
    }

    .divider {
      height: 2px;
      width: 100%;
    }
  }
</style>
